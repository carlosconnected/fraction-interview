{
  "name": "Next.js + Express + Postgres (Node 22 + pnpm)",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3000, 4000, 5432],
  "postCreateCommand": "bash -lc 'if [ -f frontend/package.json ]; then cd frontend && pnpm install; fi; if [ -f backend/package.json ]; then cd backend && pnpm install; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "streetsidesoftware.code-spell-checker"
      ]
    }
  }
}

